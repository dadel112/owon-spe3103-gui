Desktop task bar: launch button, window buttons (latched when active), etched tray with clock.

```jsx
<TaskBar startLabel="Bench" tasks={[{label:"Supply — COM3",icon:"zap"}]} activeTask="Supply — COM3" tray={["cellular-signal-3","volume-3"]} clock="14:32" />
```

Only for full-desktop mockups. `startLabel` is the product's own word — never a third party's.
