Square bevelled toolbar button holding one 16px pixel icon.

```jsx
<Toolbar><IconButton icon="save" label="Save profile" /><IconButton icon="chart" label="Log" active /></Toolbar>
```

`active` latches it (inverted bevel + 2px checkered face) — that is how the era showed a toggled tool.
