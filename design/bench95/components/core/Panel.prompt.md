The group box: 1px etched frame with an optional caption interrupting the top edge.

```jsx
<Panel label="Channel 1"><Slider value={v} onChange={setV} /></Panel>
```

This is the only grouping device in the system — no cards, no shadows, no radii. Nest at most one level deep.
