A 1px-raised strip holding IconButtons, with an etched separator and a drag grip.

```jsx
<Toolbar>
  <ToolbarGrip /><IconButton icon="play" label="Run" /><ToolbarSeparator /><IconButton icon="save" label="Save" />
</Toolbar>
```

Sits directly under the menu bar, full width, no gap.
