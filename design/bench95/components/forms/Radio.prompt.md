12px round bevelled radio with a 4px black dot. Stack vertically inside a Panel, 4px apart.

```jsx
<Radio name="mode" checked={mode==="cv"} onChange={()=>setMode("cv")} label="Constant voltage" />
```
