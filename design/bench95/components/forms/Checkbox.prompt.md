13px sunken box with a 2px black tick; label 6px to the right, whole row clickable.

```jsx
<Checkbox checked={ovp} onChange={setOvp} label="Enable OVP" />
```

There is no switch component in this system — a boolean is a checkbox, or a latched Button for output enable.
