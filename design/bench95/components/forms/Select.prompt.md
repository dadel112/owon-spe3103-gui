The combo box: sunken well, bevelled 17px arrow button, hard-outlined drop list with navy selection.

```jsx
<Select value={port} options={["COM1","COM3","/dev/ttyUSB0"]} onChange={setPort} width={150} />
```

Drop list is a hard black 1px outline with no shadow and no rounding.
