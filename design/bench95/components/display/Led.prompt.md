A round indicator lamp with a dim unlit state and a hard glow when lit.

```jsx
<Led on={connected} label="LINK" />
<Led on={tripped} color="red" blink label="OVP TRIP" />
```

Lamps are always labelled in 11px UI type, never icon-only.
