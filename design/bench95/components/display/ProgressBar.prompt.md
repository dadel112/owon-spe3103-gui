Sunken trough filled with discrete navy chunks.

```jsx
<ProgressBar value={62} label="62%" />
```

Keep `chunky` on — the stepped fill is the point. Use it for sequence progress and firmware transfer, not for live analogue values (that is a Slider or a readout).
